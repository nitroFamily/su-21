/ - content_for(:header) do
/ 	h1 class="title"
/ 		| Стена
/ 	- if params[:offset].nil? || params[:offset].to_i == 0
/ 	- else
/ 		= link_to " ", vk_posts_path(offset: params[:offset].to_i - 3), class: "icon icon-left-nav pull-left"
/ 	= link_to " ", vk_posts_path(offset: params[:offset].to_i + 3), class: "icon icon-right-nav pull-right"

/ ul class="table-view"
/ 	- @posts.each do |post|
/ 		li class="table-view-cell row"
/ 			.lesson-number
/ 				.wrapper
/ 					img src="#{post.user.photo}"
/ 			.vk-content
/ 				= truncate(post.text.gsub(/<\/?br>/, " "), length: 60)
/ 				p
/ 					i.fa.fa-clock-o
/ 					= time_ago_in_words(Time.at(post.date)) + " ago"
/ 					- if post.attachments
/ 						i.fa.fa-file-o
/ 						= attachment_type_to_word post.attachments[0].type